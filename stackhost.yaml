user:
  name: Adityaomer
  github: My-website
runtime: 
  runtimes: python
  image: "python:3.11-slim"

commands:
  package: "nano inxi"
  build:
    - "pip install -r requirements.txt"
  start: "python3 web.py"

repository:
  branch: "main"
  auto_deploy: False

env:
  - ENVIRONMENT:production