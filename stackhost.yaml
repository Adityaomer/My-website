user:
  name: Adityaomer
  github: My-website
runtime: python:3.11

commands:
  package: "nano inxi"
  build:
    - "pip install -r requirements.txt"
  start: "python3 web.py"

repository:
  branch: "main"
  auto_deploy: False

env:
  - ENVIRONMENT:production